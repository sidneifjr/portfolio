---
import Nav from './Nav.astro'

const pathname = Astro.url.pathname
---

<header
  class="w-full max-w-[656px] py-7 mx-auto fixed left-[50%] translate-x-[-50%] flex justify-between items-center"
  data-cy="header"
>
  <a href="/">
    <h1 class="font-secondary text-4xl text-white">SF</h1>
  </a>

  <Nav activeItem={pathname} />

  <slot />
</header>
